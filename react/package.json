{
  "private": true,
  "type": "module",
  "dependencies": {
    "valtio-kit": "workspace:*"
  },
  "peerDependencies": {
    "react": "*",
    "valtio": "*"
  },
  "scripts": {
    "dev": "rimraf dist && tsup --sourcemap --watch",
    "build": "rimraf dist && tsup"
  }
}