{
  "private": true,
  "type": "module",
  "dependencies": {
    "valtio-kit": "workspace:*"
  },
  "peerDependencies": {
    "react": "*",
    "valtio": "*"
  },
  "scripts": {
    "build": "rimraf dist && tsup",
    "build:dev": "watchlist src tsup.config.ts -- tsup --sourcemap"
  }
}